# scripts

Utility scripts for local development, maintenance, and CI helpers.

Current script:
- `react-spa-smoke.ps1`: validates SPA routes, API reachability, and built asset delivery against a running backend.
